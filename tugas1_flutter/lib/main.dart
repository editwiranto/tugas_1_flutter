import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(home: Tugas1_flutter()));
}

class Tugas1_flutter extends StatelessWidget {
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("SELAMAT BELAJAR"),
        ),
      ),
    );
  }
}
